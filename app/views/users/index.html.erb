
<table>
    <thead>
        <tr>
        <th>#</th>
        <th>Username</th>
        <th>Email</th>
        <th colspan ="3">Action</th>
        </tr>
    </thead>
    <tbody>
        <% @users.each do |user| %>
            <tr class='table-row'>
            <td><%= user.id %></td>
            <td><%= user.username %></td>
            <td><%= user.email %></td>
            <td class = 'index-btn'><%= link_to 'Show', user_path(user) %></td>
            <td class = 'index-btn'><%= link_to 'Edit', edit_user_path(user) %></td>
            <td class = 'index-btn'><%= link_to 'Edit', edit_user_path(user) %></td>
            </tr>
        <%end%>   
    </tbody>
</table>

<%= link_to 'New User', new_user_path %>